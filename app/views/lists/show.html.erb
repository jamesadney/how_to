<p id="notice"><%= notice %></p>

<h1><%= @list.name %></h1>

<p><%= @list.description %></p>


<div id='step'>
  <ol>
<% @list.steps.each do |step| %>

  <li><%= step.description %> <%= link_to "Edit", edit_list_step_path(@list) %> | <%= link_to "Delete", [@list, step], :method => :delete,
       :confirm => "Are you sure you want to delete this step?" %></li>
  <% end %>
</ol>
</div>

<p><%= link_to "Add a New Step", new_list_step_path(@list) %></p>

<%= link_to 'Edit List', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
